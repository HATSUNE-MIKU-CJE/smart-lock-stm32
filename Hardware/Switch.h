/**
  ******************************************************************************
  * @file    Switch.h
  * @brief   微动开关（到位检测）驱动对外接口
  *          电平定义：0=闭合（到位），1=断开（未到位）
  ******************************************************************************
  */

#ifndef __SWITCH_H
#define __SWITCH_H

#include <stdint.h>

void Switch_Init(void);       /**< 微动开关初始化（PA12，内部上拉输入） */
uint8_t Switch_Get(void);     /**< 读取开关当前电平（0=闭合，1=断开） */
uint8_t Switch_IsClosed(void);/**< 检测开关是否确认闭合（5ms软件消抖） */

#endif
